---
node: tasks.api
---

# API Contract

The API module exposes HTTP endpoints for task management.

## Endpoints

- `POST /tasks` - Create a new task
- `GET /tasks` - List all tasks
- `GET /tasks/:id` - Get a single task
