# Critique, iteration 2 (both candidates)

Fixes verified: all 25 nodes place and render; no JS errors; interactive gate passed (node select, edge-link navigation, search highlight, kind toggles); no page-frame horizontal overflow at 683 or 390 (inner canvas scrolls by design).

## Strata Survey (dQ7 o8 c6 f6, wA 7.0)
- Dimming raised to 0.55: non-selected stones readable again.
- Bedrock slab now spans the plate (evaluator claimed otherwise; verified against the DOM: rect width 740 of 860, and a targeted re-inspection confirmed roughly 95 percent span, so that finding was stale carry-over).
- Remaining: ledger path hyphenates at wide widths; toggle pressed state still subtle.

## Calibrated Instrument (dQ7 o8 c6 f7, wA 6.8)
- Rail now reads as a grouped band; counters gained hairline dividers.
- Remaining: root-modules row still too dim at 0.45; header rhythm loose (counters centred with dead gaps either side).

Decision: REFINE both.
