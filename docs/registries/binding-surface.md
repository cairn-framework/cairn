# Binding surface allowlist

This ordered allowlist defines repository surfaces that require a binding decision. Each row is an exact path or a directory prefix ending in `/`; globs are not supported. Extending this registry is a binding decision by construction.

- docs/spec.md
- docs/registries/
- tools/agent-pack/content/
- src/artefacts/registry/
- cairn.blueprint
