# Subsystem Cards With ICD Table

**Source:** _resized/40.png
**Captured:** 2026-04-28 (getcairn.dev web UI)

## Summary
A close-up view of the system architecture canvas shows three subsystem cards rendered in a row, with a tooltip or detail panel partially overlapping the rightmost card. Connecting lines route between the cards, one carrying an inline label "thernet / IP" (truncated "Ethernet / IP"). Below the canvas, an Interface Control Document table lists two connections with IDs, source-to-target direction, protocol, and signal payload columns. The view conveys how subsystem nodes on the diagram are paired with a tabular ICD that enumerates wires and protocols.

## UI inventory
- Subsystem card 1: header tag "SUBSYSTEM", title "Launch & Recovery System", code "SUB-LARS"
- Subsystem card 2: header tag "SUBSYSTEM", title "Communications & Data Link", code "SUB-COMMS"
- Subsystem card 3 (partially obscured): header tag "SUBSYSTEM", title fragment "Auto..." / "Mi..." (Autonomy, Control & Mission Payload, inferred from row below)
- Inline edge label between card 2 and card 3: "thernet / IP" (truncated "Ethernet / IP")
- Connector line with small green circle anchor near top of card 2 routing up and over to card 3
- Floating panel overlapping card 3 with six light tan rectangles arranged in a 3x2 grid (likely component or signal preview tiles)
- Section header below canvas: "INTERFACE CONTROL DOCUMENT  2 connections"
- Disclosure caret on the right edge of the section header
- Table column headers (partially clipped on left): "D", "CONNECTION", "PROTOCOL", "SIGNALS"
- Row 1: "F-001" (likely "ICF-001"), "Power Generation & Distribution to ROV Vehicle", protocol block "HVDC power + fibre-optic data", signals "tether_hvdc_power, rov_video_sonar_data,..."
- Row 2: "F-002" (likely "ICF-002"), "Autonomy, Control & Mission Payload to Communications & Data Link", protocol block "Ethernet / IP", signals "compressed_video_stream, compressed_sona..."

## Novel observations
- New term "INTERFACE CONTROL DOCUMENT" appears as a first-class section, abbreviated as ICF in row IDs (Interface Control Form/File).
- Connection IDs use prefix "ICF-###" rather than the previously seen ART-/CON-/REQ- patterns.
- Protocol cell uses a multi-line bordered token-style block (e.g., "HVDC power + fibre-optic data" wrapped over four lines), distinct from plain text cells.
- Signal payloads use snake_case identifiers (tether_hvdc_power, rov_video_sonar_data, compressed_video_stream, compressed_sonar...).
- Edge labels are rendered inline on the connector line itself rather than in a side panel.
- Small green circle marker on card 2 suggests a port or connection anchor distinct from the line endpoint.
- Floating tile panel (3x2 grid) appears as a hover or expansion overlay over a card, hinting at a "drill into subsystem" interaction.

## Suggested filename
40-subsystem-icd-connections.png
