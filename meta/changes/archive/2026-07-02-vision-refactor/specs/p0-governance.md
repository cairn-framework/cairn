# Spec: Phase 0 — Governance

## Acceptance criteria

- `cargo run --release --bin cairn -- lint` reports no Error findings.
- The five decision artefacts and the research artefact load and are visible
  via `cairn decisions <node>` for their attached nodes.
- `docs/spec.md` is at v0.8 with a changelog entry naming all five decisions.
